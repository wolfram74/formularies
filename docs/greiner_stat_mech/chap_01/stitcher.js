
var equations = []
equations = equations.concat(jacob_equations)
equations = equations.concat(eric_equations)
equations = equations.concat(bret_equations)
equations = equations.concat(peter_equations)
